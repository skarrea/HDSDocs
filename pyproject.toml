[project]
name = "hdsdocs"
version = "0.1.0"
description = "Documentation for the linux servers at HDS"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
]

[dependency-groups]
dev = [
    "zensical>=0.0.27",
]
